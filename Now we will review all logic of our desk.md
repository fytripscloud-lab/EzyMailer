

few thing you have to check and impliment if missing (If required create and impliment API, DB table and UI).
1. start campaign button only enable if all criteria are met.
2. Validate user input fields to ensure they are not empty or invalid before proceeding.
3. Signin to continue fields, username and password by default should be empty and should not allow any whitespace characters.4. Implement error messages for invalid inputs to guide users in correcting their mistakes.
5. also remove text on the login page locallogin admin/admin
6. after login , on the top right after campaign tab, need a button called reset all. once user clicks on reset all, it should clear all the data entered in the campaign form and reset the form to its default state (Empty customer emails, subjects+body, attach content).
7. if a user tries to start a campaign without filling in all required fields, display a warning message indicating which fields are missing or invalid.
8. if a user logged in a device mac or windows, at the same time if user want to login to other device, then ask user on the login page, you are already logged in on another device. Do you want to log out from the other device and continue? (Yes/No). If user selects Yes, log out from the other device and allow login on the current device. If No, stay on the current page and do not allow login. at the moment user new system will remove all content (Data, subject+body, attachments) from the campaign form and reset it to default state. We need to ensure that this behavior is clearly communicated to the user before they proceed with logging in on a new device. if user logout and login with the sane device but different credentials, then also need to clear all content from the campaign form and reset it to default state. We need to ensure that this behavior is clearly communicated to the user before they proceed with logging in with different credentials on the same device but at the monent all user settings that stored in our dedicated database this will automatically fetched to the local system and fetched the settings on the desktop app mac or windows.

9. Remember data, subjects + boy and attach content should store in the local storage or local database, other settings and user preferences should be stored in the dedicated database to ensure that user settings are preserved across different devices and sessions.
10. all tags must store in the dedicated database and should be fetched to the local system when the user logs in. This ensures that all tags are consistent across different devices and sessions.
11. supposed user start browser 2, and data (customer emails entered 200) as per default settings you have to segrigate the data 50% of each gmail windows, must flow the sending setings to send one by one email. out automation must be like compose and send all emails. once the process started must show pause option, also cancel option. and campaign progress bar must show the progress of the campaign, including the number of emails sent, remaining emails, and any errors encountered during the sending process. Additionally, provide an option to resume the campaign if it was paused or interrupted.
12. active gmail windows must show the count windows. 
13. Dynamic tags must be dynamically generated (Randomly generated) and should be unique for each campaign. Ensure that the dynamic tags are properly integrated into the email content and that they function as intended when the emails are sent.
14. Implement a feature to allow users to preview the email content before sending the campaign. This preview should accurately reflect how the email will appear to recipients, including any dynamic tags and formatting. also start confirmation is required before sending the campaign, asking users to confirm that they want to proceed with sending the emails.
